{
  "name": "soba",
  "version": "0.0.0",
  "main": "index.js",
  "author": "Yixun <snowfennard@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "client:start": "yarn workspace @soba/client start",
    "client:build": "yarn workspace @soba/client build",
    "client:test": "yarn workspace @soba/client test",
    "server:build": "yarn workspace @soba/server build",
    "server:start": "yarn workspace @soba/server start"
  },
  "dependencies": {
    "prettier": "^2.5.1"
  }
}
